services:
  - type: web
    name: haps-wind-model
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirement.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
